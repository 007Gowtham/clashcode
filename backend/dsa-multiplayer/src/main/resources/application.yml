spring:
  application:
    name: dsa-multiplayer
  profiles:
    active: dev
  mail:
    host: smtp.gmail.com
    port: 587
    username: gowthams200521@gmail.com
    password: ruxwcakwiildztxm
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
  datasource:
    url: jdbc:postgresql://ep-mute-poetry-ao66bnri.c-2.ap-southeast-1.aws.neon.tech/neondb?sslmode=require
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  cloud:
    aws:
      region:
        static: ap-south-1
      credentials:
        access-key: ${AWS_ACCESS_KEY_ID:}
        secret-key: ${AWS_SECRET_ACCESS_KEY:}

server:
  port: ${SERVER_PORT:5000}
  error:
    include-message: always
    include-binding-errors: always

app:
  jwt:
    secret: ${JWT_SECRET:404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970}
    access-token-expiration: ${JWT_ACCESS_EXP_MS:900000}       # 15 min
    refresh-token-expiration: ${JWT_REFRESH_EXP_MS:604800000}  # 7 days
  email:
    cooldown-seconds: ${EMAIL_COOLDOWN_SECONDS:60}
    verify-code-expiry-hours: ${VERIFY_CODE_EXPIRY_HOURS:24}
    reset-code-expiry-minutes: ${RESET_CODE_EXPIRY_MINUTES:15}
  cors:
    allowed-origins: ${CLIENT_URL:http://localhost:3000}
  sandbox:
    image-prefix: ${SANDBOX_IMAGE_PREFIX:clashcode}
    cpus: ${SANDBOX_CPUS:0.5}
    timeout-ms: ${SANDBOX_TIMEOUT_MS:5000}
  sqs:
    judge-queue: compiler-queue
  s3:
    bucket: clashcode-profiles-357919579159-ap-south-1-an
