spring:
  datasource:
    # ── Neon PostgreSQL ───────────────────────────────────────────────────────
    # TODO: move credentials to OS env vars (DB_USERNAME / DB_PASSWORD) once confirmed working
    url: jdbc:postgresql://ep-mute-poetry-ao66bnri.c-2.ap-southeast-1.aws.neon.tech/neondb?sslmode=require
    username: ${DB_USERNAME:neondb_owner}       # set DB_USERNAME in OS env to avoid hardcode
    password: ${DB_PASSWORD:npg_ANsfC6kjbdw2}   # set DB_PASSWORD in OS env to avoid hardcode
    driver-class-name: org.postgresql.Driver
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  flyway:
    enabled: true
    baseline-on-migrate: true

logging:
  level:
    com.clashcode: DEBUG
    org.springframework.security: DEBUG
